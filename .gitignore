node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results

# ── The vault is private ────────────────────────────────────────────────
# The repo is public so it can be taught from; research output, reading
# history, notes and drafts must never enter git. Anything a feature writes
# to disk gets added here in the same commit.
/vault
/data
*.local.*
.env*
